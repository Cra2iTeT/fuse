<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fuse.mapper.ErrorLogMapper">

    <insert id="save" parameterType="com.fuse.domain.pojo.ErrorLog">
        insert into error_log (log_id, error_time, error_msg, error_type)
        values (#{logId}, #{errorTime}, #{errorMsg}, #{errorType}) ON DUPLICATE KEY
        UPDATE
            log_id=#{logId},
            error_time= #{errorTime},
            error_msg = #{errorMsg},
            error_type = #{errorType}
    </insert>

</mapper>
